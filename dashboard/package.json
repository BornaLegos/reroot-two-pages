{
  "name": "modular-style",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "node-sass index.scss --style compressed -o dist",
    "start": "npm run build && node-sass --watch index.scss --style compressed -o dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "material-design-icons": "^3.0.1"
  },
  "devDependencies": {
    "node-sass": "^6.0.1"
  }
}
